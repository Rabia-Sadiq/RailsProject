<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Your Favorite Colors</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    body {
      background: url('https://www.transparenttextures.com/patterns/paper-fibers.png');
      background-color: #fdf6e3;
      font-family: 'Georgia', serif;
      color: #4b3f30;
    }
    .text-shadow {
      text-shadow: 1px 1px 2px black;
    }
  </style>
</head>
<body class="min-h-screen p-8">
<nav>
        <div class="nav-container">
            <a href="#" class="nav-logo">Color&Fit</a>
            <ul class="nav-menu">
          <li class="nav-item"><%= link_to "Home", root_path, class: "nav-link " %></li>
          <li class="nav-item"><%= link_to "About", about_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Quiz", undertone_quiz_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "OOTD", ootd_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "WishList", favorite_dresses_path, class: "nav-link" %></li>

          <li class="nav-item"><%= link_to "Profile", profile_path, class: "nav-link" %></li>
 <li class="nav-item">
  <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link active", form_class: "d-inline" %>
          </li>
          </ul>
            <button class="mobile-menu-btn">☰</button>
        </div>
    </nav>
  <div class="max-w-5xl mx-auto bg-white bg-opacity-80 border-4 border-yellow-800 rounded-xl shadow-lg p-10">
    <h1 class="text-3xl font-bold mb-6 text-center">Your Favorite Colors</h1>

    <% if @favorite_colors.any? %>
      <div class="flex flex-wrap justify-center gap-6">
        <% @favorite_colors.each do |color| %>
          <div class="flex flex-col items-center space-y-2">
            <div class="w-24 h-24 rounded-lg border-2 border-yellow-700 shadow-md flex items-center justify-center"
                 style="background-color: <%= color.hex %>;">
              <span class="text-white font-bold text-shadow"><%= color.name %></span>
            </div>

            <%= button_to "❌ Remove", favorite_color_path(color), method: :delete,
                  data: { confirm: "Are you sure?" },
                  class: "bg-red-700 text-white px-3 py-1 rounded hover:bg-red-800 transition duration-200" %>
          </div>
        <% end %>
      </div>
    <% else %>
      <p class="text-center text-lg mt-6">You haven't added any favorite colors yet.</p>
    <% end %>

    <!-- Back to Palette -->
    <div class="text-center mt-10">
      <%= link_to "← Back to Palette", palette_path,
            class: "inline-block bg-green-800 hover:bg-green-900 text-white font-semibold py-2 px-6 rounded-lg shadow-lg transition duration-200" %>
    </div>
  </div>

</body>
</html>
