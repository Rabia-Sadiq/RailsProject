<div id="profile" style="max-width:400px; margin:40px auto; padding:30px; background:#fff8f0; border-radius:12px; box-shadow:0 4px 8px rgba(0,0,0,0.1); font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;">
  <h1 style="text-align:center; margin-bottom:24px; color:#b25c2f;">User Profile</h1>

  <p style="display:flex; justify-content:space-between; font-weight:600; font-size:16px; color:#7a4a1b; margin-bottom:18px;">
    <strong>Name:</strong> <span><%= @user.name.presence || "N/A" %></span>
  </p>
  <p style="display:flex; justify-content:space-between; font-weight:600; font-size:16px; color:#7a4a1b; margin-bottom:18px;">
    <strong>Undertone:</strong> <span><%= @user.undertone.presence || "N/A" %></span>
  </p>
  <p style="display:flex; justify-content:space-between; font-weight:600; font-size:16px; color:#7a4a1b; margin-bottom:18px;">
    <strong>Weight (kg):</strong> <span><%= @user.weight.presence || "N/A" %></span>
  </p>
  <p style="display:flex; justify-content:space-between; font-weight:600; font-size:16px; color:#7a4a1b; margin-bottom:18px;">
    <strong>Height (cm):</strong> <span><%= @user.height.presence || "N/A" %></span>
  </p>

  <%= link_to "Edit Profile", edit_profile_path, 
      style: "display:block; width:100%; padding:12px; background:#b25c2f; color:#fff; border:none; border-radius:8px; font-weight:600; font-size:16px; text-align:center; text-decoration:none; cursor:pointer; transition: background-color 0.3s;", 
      onmouseover: "this.style.background='#9e4a28'", 
      onmouseout: "this.style.background='#b25c2f'" %>

  <% if notice %>
    <p style="color:#2e7d32; font-size:14px; margin-top:10px; text-align:center;"><%= notice %></p>
  <% end %>

  <% if alert %>
    <p style="color:#d32f2f; font-size:14px; margin-top:10px; text-align:center;"><%= alert %></p>
  <% end %>
</div>
